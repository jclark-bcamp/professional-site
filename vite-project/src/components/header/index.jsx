import Nav from "../nav"; 

function Header() {
    // const { currentPage, handlePageChange } = props;
    return (
        <header>
            <section>
                <h1>Janessa Clark</h1>
                <Nav />
            </section>
        </header>
    );
}

export default Header;  // Exporting the Header component